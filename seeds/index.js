const  = require('./');
const  = require('./');
const  = require('./');
const  = require('./')
const sequelize = require('../config/connection');

const seedAll = async () => {

    process.exit(0);
};

seedAll();